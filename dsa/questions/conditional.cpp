#include <iostream>
using namespace std;

int main(){

    //? Check the character and print the type of character
    /*
    char a;
    a = cin.get();
    int b = (int) a;
    cout << b;
    if (b >= 97 && b <= 123)
    {
        cout << a << " is lowercase " << endl;
    }
    else if (b >= 65 && b <= 91)
    {
        cout << a << " is uppercase" << endl;
    }
    else if (b >= 48 && b <= 58)
    {
        cout << a << " is numberic " << endl;
    }
    */

};